# B3-1 학습 노트: 정보를 엄청 빠르게 찾아주는 작은 저장소 만들기

> 문과 중졸도 이해할 수 있게

## 📖 목차
1. 초심자를 위한 용어집
2. 과제 해석 및 분석
3. 과제를 진행하기 위한 기초
4. 각 기초를 익히기 위한 간단한 체험 예제
5. 과제를 작게 쪼개기
6. 워크플로우별 트레이드오프, 이슈, 트러블슈팅
7. 과제 완료 후 학습한 내용 정리

## 1. 초심자를 위한 용어집

| 용어 | 설명 | 비유 |
|------|------|------|
| 해시 함수 | 문자열을 숫자로 변환 | 우편번호 (주소→번호) |
| 해시 테이블 | 키→값을 빠르게 저장/조회 | 우편번호 사전 |
| 충돌 | 서로 다른 키가 같은 해시값 | 두 집이 같은 우편번호 |
| 체이닝 | 충돌 시 연결 리스트로 연결 | 같은 우편번호면 줄 세우기 |
| O(1) | 입력 크기와 무관하게 일정 시간 | 우편번호로 바로 찾기 |
| dict 금지 | Python 내장 사전 사용 안 함 | 계산기 없이 암산하기 |

## 2. 과제 해석
한 줄: dict를 쓰지 말고 해시 테이블을 직접 구현하라.
핵심: "편한 도구를 빼앗아 원리를 이해하게 함"

## 3. 기초
1. 배열과 인덱스 접근
2. 해시 함수 (모듈러 연산, 분산)
3. 충돌 해결 (체이닝, 개방주소법)
4. 시간 복잡도 (O(1) vs O(n))
5. 캐싱 (LRU)

## 4. 체험 예제
### 해시 함수
```python
def hash_func(key, size=10):
    total = sum(ord(c) for c in key)
    return total % size
```

### 충돌 해결 (체이닝)
```python
table = [[] for _ in range(10)]
idx = hash_func("apple")
table[idx].append(("apple", 100))
```

## 5. 잡/워크
- Job 1: 해시 함수 구현
- Job 2: put/get 연산
- Job 3: 충돌 해결 (체이닝)
- Job 4: 캐싱 (LRU)
- Job 5: 테스트

## 6. 트레이드오프
- 체이닝 vs 개방주소법: 체이닝 선택 (구현 단순, 메모리 유연)
- 해시 함수: 단순 합산 vs 곱셈 혼합 — 분산성 고려

## 7. 학습 정리
- "dict가 어떻게 O(1)을 달성하는지" — 해시 함수 + 배열
- "충돌은 피할 수 없지만 관리할 수 있다" — 체이닝/개방주소법
