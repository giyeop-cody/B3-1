# eval 브랜치 — 비제출 자료 (참고용)

이 브랜치는 **제출 대상이 아닌** 과제 출제 원본 문서를 보관한다.
실제 제출물(구현 코드/테스트/문서)은 `master` 브랜치에 있다.

## 파일

- `미션-mini-redis-구축.md` : 원본 미션(과제) 명세
- `평가문항.md`             : 원본 평가 문항

## 브랜치 안내

```bash
git checkout master   # 제출용 구현물
git checkout eval      # (현재) 비제출 참고 자료
```
